float chia(float a, float b) {
    if (b == 0) {
        cout << "Khong the chia cho 0!" << endl;
        return 0; // Hoặc bạn có thể xử lý trường hợp này theo cách khác
    } else {
        float thuong = a / b;
        return thuong
    }
}
